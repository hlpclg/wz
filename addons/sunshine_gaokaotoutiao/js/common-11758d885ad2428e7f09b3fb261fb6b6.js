// Generated by CoffeeScript 1.9.2
(function() {
  $(function() {
    var height;
    height = $(window).height();
    $('html').height(height);
    $('body').height(height);
    $('input').blur(function() {
      var $this;
      $this = $(this);
      if (!$this.val()) {
        $this.css("border", "1px solid red");
        return $this.attr("placeholder", "不能为空");
      }
    });
    $('input').focus(function() {
      var $this;
      $this = $(this);
      $this.css("border", "1px solid #b5b5b5");
      if ($this.hasClass('name')) {
        $this.attr("placeholder", "输入你或朋友的名称");
      }
      if ($this.hasClass('area')) {
        return $this.attr("placeholder", "输入所在地区");
      }
    });
    return $('#link').on('click', function(e) {
      var area, maximum, minimum, name, randomnumber;
      e.preventDefault();
      name = $('.name').val();
      area = $('.area').val();
      if (name && area) {
        maximum = 48;
        minimum = 0;
        randomnumber = Math.floor(Math.random() * (maximum - minimum + 1)) + minimum;
        return window.location.href = "./headline_news?name=" + name + "&area=" + area + "&contentId=" + randomnumber;
      }
    });
  });

}).call(this);
